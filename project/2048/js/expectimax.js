// returns the optimal move from the given state
function expectimax(grid) {
    // stand-in function picks a random move
    /*
    up: 0
    right: 1
    down: 2
    left: 3
     */

    return Math.floor(Math.random() * 4);
}